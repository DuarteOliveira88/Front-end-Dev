Comando para criar o cabeçalho html -> ! + "enter";
Para realizar calculas dentro de uma string: 'Exemplo de string ${num+num}'

shift+fn+setacima ou baixo-> copia a linha

css
//tira bordas brancas(extende a pagina)
.content-area {
    width: 100vw;  /* Define a largura total da tela */
    margin-left: calc(-50vw + 50%);  /* Centraliza o container */
    margin-right: calc(-50vw + 50%);
}
